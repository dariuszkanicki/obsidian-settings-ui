import type { Options } from "prettier";

const config: Options = {
  semi: true,
  singleQuote: true,
  trailingComma: "all",
  bracketSpacing: true,
  printWidth: 140,
  tabWidth: 2,
  endOfLine: "auto",
};

export default config;
